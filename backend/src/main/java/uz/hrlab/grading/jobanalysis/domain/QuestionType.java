package uz.hrlab.grading.jobanalysis.domain;

/** Question types supported by the MVP 1 job-analysis questionnaire. */
public enum QuestionType {
    TEXT,
    LONG_TEXT,
    SINGLE_CHOICE,
    MULTI_CHOICE,
    RATING_SCALE,
    NUMBER
}

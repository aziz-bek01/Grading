package uz.hrlab.grading.organization.domain;

/**
 * Universal department type — never bank-specific (architecture §3).
 */
public enum DepartmentType {
    BRANCH,
    DEPARTMENT,
    DIVISION,
    UNIT
}

node_modules/
dist/
coverage/
.vite/

.idea/
.vscode/
.DS_Store
Thumbs.db

.git/
.gitignore
.gitattributes

*.log
npm-debug.log*

.env
.env.*
!.env.example
# .env.production carries ONLY non-secret build flags (VITE_DEV_AUTH=false,
# VITE_USE_MSW=false) that `vite build` must read — keep it in the build context.
!.env.production

# Tests not needed in container (CI runs them outside)
src/**/*.test.ts
src/**/*.test.tsx
src/**/*.spec.ts
src/**/*.spec.tsx

README.md
